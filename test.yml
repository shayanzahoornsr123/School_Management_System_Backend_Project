config:
  target: "http://localhost:5050"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - flow:
      - post:
          url: "/api/auth/login"
          json:
            email: "test@test.com"
            password: "123456"